# 🎓 دليل تكامل المحرر الرياضي المرئي

## 📚 المحتويات
1. [نظرة عامة](#نظرة-عامة)
2. [التقنيات المستخدمة](#التقنيات-المستخدمة)
3. [التثبيت](#التثبيت)
4. [الاستخدام في لوحة التحكم](#الاستخدام-في-لوحة-التحكم)
5. [العرض في التطبيق](#العرض-في-التطبيق)
6. [أمثلة عملية](#أمثلة-عملية)
7. [حل المشاكل](#حل-المشاكل)

---

## 🎯 نظرة عامة

تم بناء نظام متكامل لإنشاء وعرض المعادلات الرياضية:

### **لوحة التحكم (Admin Dashboard):**
- ✅ محرر مرئي تفاعلي باستخدام **MathLive**
- ✅ أزرار سريعة للرموز الرياضية
- ✅ معاينة مباشرة
- ✅ حفظ LaTeX في قاعدة البيانات

### **التطبيق (Student App):**
- ✅ عرض احترافي باستخدام **MathJax**
- ✅ دعم React Native كامل
- ✅ دعم RTL للعربية

---

## 🔧 التقنيات المستخدمة

| المكون | التقنية | الغرض |
|--------|---------|-------|
| **المحرر** | MathLive | إنشاء المعادلات مرئياً |
| **العرض (Web)** | MathJax | عرض LaTeX في المتصفح |
| **العرض (Mobile)** | MathJax + WebView | عرض LaTeX في React Native |
| **التخزين** | LaTeX (نص عادي) | حفظ في قاعدة البيانات |

---

## 📦 التثبيت

### **1. لوحة التحكم:**

```bash
cd admin-dashboard
npm install mathlive --legacy-peer-deps
```

### **2. التطبيق (React Native):**

```bash
cd ..
npm install react-native-webview
```

---

## 🖥️ الاستخدام في لوحة التحكم

### **1. استيراد المكون:**

```tsx
import MathEditor from '@/components/MathEditor';
```

### **2. استخدام المحرر:**

```tsx
'use client';

import { useState } from 'react';
import MathEditor from '@/components/MathEditor';

export default function QuestionForm() {
  const [equation, setEquation] = useState('');

  const handleSave = async () => {
    // حفظ LaTeX في قاعدة البيانات
    const questionData = {
      question_text: 'احسب قيمة المعادلة التالية:',
      equation_latex: equation, // LaTeX string
    };
    
    // حفظ في Supabase
    await supabase.from('questions').insert(questionData);
  };

  return (
    <div className="space-y-4">
      <label className="block text-sm font-medium">
        المعادلة الرياضية:
      </label>
      
      <MathEditor
        value={equation}
        onChange={setEquation}
        placeholder="أدخل المعادلة..."
      />
      
      <button onClick={handleSave}>
        حفظ السؤال
      </button>
    </div>
  );
}
```

---

## 📱 العرض في التطبيق

### **1. استيراد المكون:**

```javascript
import MathRenderer from '../components/MathRenderer';
```

### **2. عرض المعادلة:**

```javascript
import React from 'react';
import { View, Text } from 'react-native';
import MathRenderer from '../components/MathRenderer';

export default function QuestionScreen({ question }) {
  return (
    <View style={{ padding: 20 }}>
      <Text style={{ fontSize: 18, marginBottom: 10 }}>
        {question.question_text}
      </Text>
      
      {/* عرض المعادلة */}
      <MathRenderer 
        latex={question.equation_latex}
        style={{ marginVertical: 10 }}
      />
    </View>
  );
}
```

### **3. التكامل مع HtmlRenderer:**

```javascript
// في HtmlRenderer.js - تم التكامل تلقائياً
// يكتشف LaTeX ويعرضه باستخدام MathRenderer
```

---

## 💡 أمثلة عملية

### **مثال 1: كسر بسيط**

**في المحرر:**
- اضغط زر "½"
- اكتب `2` في البسط
- اكتب `3` في المقام

**LaTeX المحفوظ:**
```latex
\frac{2}{3}
```

**في التطبيق:**
```
2
─
3
```

---

### **مثال 2: معادلة تربيعية**

**في المحرر:**
- اكتب: `x`
- اضغط زر "x²"
- اكتب: `2`
- اكتب: `+ 5x + 6 = 0`

**LaTeX المحفوظ:**
```latex
x^{2} + 5x + 6 = 0
```

**في التطبيق:**
```
x² + 5x + 6 = 0
```

---

### **مثال 3: جذر تربيعي**

**في المحرر:**
- اضغط زر "√"
- اكتب: `x^2 + y^2`

**LaTeX المحفوظ:**
```latex
\sqrt{x^{2} + y^{2}}
```

**في التطبيق:**
```
√‾‾‾‾‾‾‾
 x² + y²
```

---

### **مثال 4: كسر معقد**

**في المحرر:**
- اضغط زر "½"
- في البسط: `2 \times 333`
- في المقام: `9 \times 1.01`

**LaTeX المحفوظ:**
```latex
\frac{2 \times 333}{9 \times 1.01}
```

**في التطبيق:**
```
2 × 333
───────
9 × 1.01
```

---

### **مثال 5: متباينة**

**في المحرر:**
- اكتب: `x`
- اضغط زر "≥"
- اكتب: `3`

**LaTeX المحفوظ:**
```latex
x \geq 3
```

**في التطبيق:**
```
x ≥ 3
```

---

## 🎨 الرموز المتاحة

| الزر | الرمز | LaTeX | الاستخدام |
|------|-------|-------|-----------|
| ½ | كسر | `\frac{a}{b}` | كسور |
| √ | جذر | `\sqrt{x}` | جذر تربيعي |
| x² | أس | `x^{2}` | أسس |
| x₂ | منخفض | `x_{2}` | منخفض |
| × | ضرب | `\times` | عملية ضرب |
| ÷ | قسمة | `\div` | عملية قسمة |
| ± | زائد/ناقص | `\pm` | زائد أو ناقص |
| ≤ | أصغر/يساوي | `\leq` | أصغر من أو يساوي |
| ≥ | أكبر/يساوي | `\geq` | أكبر من أو يساوي |
| ≠ | لا يساوي | `\neq` | عدم المساواة |
| ∑ | مجموع | `\sum` | مجموع |
| ∫ | تكامل | `\int` | تكامل |
| π | باي | `\pi` | ثابت باي |
| ∞ | لا نهاية | `\infty` | لا نهاية |

---

## 💾 هيكل البيانات

### **في قاعدة البيانات:**

```sql
CREATE TABLE questions (
  id UUID PRIMARY KEY,
  question_text TEXT,
  equation_latex TEXT,  -- LaTeX كنص عادي
  created_at TIMESTAMP
);
```

### **مثال على البيانات المحفوظة:**

```json
{
  "id": "123e4567-e89b-12d3-a456-426614174000",
  "question_text": "احسب قيمة المعادلة التالية:",
  "equation_latex": "\\frac{2 \\times 333}{9 \\times 1.01}",
  "created_at": "2024-12-14T12:00:00Z"
}
```

---

## 🔒 الأمان

### **التحقق من الصحة:**

```typescript
// في الـ API Route
export async function POST(request: Request) {
  const { equation_latex } = await request.json();
  
  // التحقق من LaTeX
  if (!equation_latex || typeof equation_latex !== 'string') {
    return Response.json({ error: 'Invalid LaTeX' }, { status: 400 });
  }
  
  // تنظيف من HTML/Scripts
  const cleanLatex = equation_latex
    .replace(/<script[^>]*>.*?<\/script>/gi, '')
    .replace(/<[^>]+>/g, '');
  
  // حفظ في قاعدة البيانات
  await supabase.from('questions').insert({
    equation_latex: cleanLatex
  });
}
```

---

## 🐛 حل المشاكل

### **المشكلة 1: المحرر لا يظهر**

**الحل:**
```tsx
// تأكد من استخدام 'use client' في Next.js
'use client';

import MathEditor from '@/components/MathEditor';
```

---

### **المشكلة 2: المعادلة لا تظهر في التطبيق**

**الحل:**
```javascript
// تأكد من تثبيت react-native-webview
npm install react-native-webview

// في iOS
cd ios && pod install
```

---

### **المشكلة 3: LaTeX غير صحيح**

**الحل:**
```javascript
// تحقق من LaTeX في المحرر
console.log('LaTeX:', equation);

// يجب أن يبدأ بـ \ وليس \\
// صحيح: \frac{2}{3}
// خطأ: \\frac{2}{3}
```

---

### **المشكلة 4: الأداء بطيء**

**الحل:**
```javascript
// استخدم React.memo للمكونات
export default React.memo(MathRenderer);

// تحميل MathJax مرة واحدة فقط
const [mathJaxLoaded, setMathJaxLoaded] = useState(false);
```

---

## 📊 مقارنة الحلول

| الميزة | الحل الحالي (MathLive + MathJax) | الحل القديم (HTML + Custom) |
|--------|-----------------------------------|------------------------------|
| **سهولة الاستخدام** | ⭐⭐⭐⭐⭐ | ⭐⭐ |
| **الجودة** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| **الأداء** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **المرونة** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| **الدعم** | ⭐⭐⭐⭐⭐ | ⭐⭐ |

---

## 🚀 الخطوات التالية

### **1. تحديث صفحة الدروس:**

```tsx
// في app/subjects/[id]/lessons/page.tsx
import MathEditor from '@/components/MathEditor';

// استبدال RichTextEditor بـ MathEditor للمعادلات
<MathEditor
  value={currentQuestion.equation_latex}
  onChange={(latex) => setCurrentQuestion({
    ...currentQuestion,
    equation_latex: latex
  })}
/>
```

### **2. تحديث ExamScreen:**

```javascript
// في src/screens/ExamScreen.js
import MathRenderer from '../components/MathRenderer';

// عرض المعادلة
{currentQuestion.equation_latex && (
  <MathRenderer latex={currentQuestion.equation_latex} />
)}
```

### **3. إضافة حقل equation_latex:**

```sql
-- في Supabase
ALTER TABLE questions 
ADD COLUMN equation_latex TEXT;

ALTER TABLE exam_questions 
ADD COLUMN equation_latex TEXT;
```

---

## 📝 ملاحظات مهمة

1. **LaTeX vs HTML:**
   - LaTeX أفضل للمعادلات المعقدة
   - HTML أفضل للنصوص البسيطة

2. **الأداء:**
   - MathJax يحمل مرة واحدة
   - WebView تستخدم للعرض في React Native

3. **التوافق:**
   - يعمل على iOS و Android
   - يدعم RTL كاملاً

4. **الأمان:**
   - LaTeX نص عادي (آمن)
   - لا يحتوي على HTML أو Scripts

---

## 🎯 الخلاصة

تم بناء نظام متكامل وآمن واحترافي لإنشاء وعرض المعادلات الرياضية:

✅ **محرر مرئي** سهل الاستخدام  
✅ **عرض احترافي** في التطبيق  
✅ **أداء ممتاز** وسريع  
✅ **آمن** بدون HTML  
✅ **متوافق** مع جميع الأجهزة  

**استخدمه الآن!** 🚀
