# 📐 دليل استخدام المحرر الرياضي

## 🎯 المحرر يدعم الآن:

### ✅ **1. الكسور الرياضية**
### ✅ **2. الجذور التربيعية**
### ✅ **3. الأسس والقوى**
### ✅ **4. الكسور المتداخلة**

---

## 📝 كيفية الكتابة:

### **1. الكسور البسيطة:**

**اكتب:**
```
2/3
```

**يظهر كـ:**
```
2
─
3
```

---

### **2. كسور معقدة:**

**اكتب:**
```
احسب قيمة 2×333 / 9×1.01 تقريباً ؟
```

**يظهر كـ:**
```
احسب قيمة ٢×٣٣٣
           ─────  تقريباً ؟
           ٩×١٫٠١
```

---

### **3. الجذور التربيعية:**

**طريقة 1 - استخدام الرمز:**
```
احسب √(x²+y²)
```

**طريقة 2 - استخدام sqrt:**
```
احسب sqrt(x²+y²)
```

**يظهر كـ:**
```
احسب √‾‾‾‾‾‾‾
      x²+y²
```

---

### **4. الأسس والقوى:**

**طريقة 1 - استخدام ^:**
```
x^2 + y^3
```

**طريقة 2 - استخدام الرموز:**
```
x² + y³
```

**يظهر كـ:**
```
x² + y³
```

---

### **5. معادلات معقدة (كل شيء معاً):**

**اكتب:**
```
احسب √(x²+y²) / 2×3 تقريباً ؟
```

**يظهر كـ:**
```
احسب √‾‾‾‾‾‾‾
      x²+y²
      ─────  تقريباً ؟
       2×3
```

---

## 🔤 الرموز المدعومة:

| الرمز | الاستخدام | مثال |
|------|----------|------|
| `/` | كسر | `2/3` |
| `√` | جذر تربيعي | `√(16)` |
| `sqrt()` | جذر تربيعي | `sqrt(16)` |
| `^` | أس | `x^2` |
| `²` | تربيع | `x²` |
| `³` | تكعيب | `x³` |
| `×` | ضرب | `2×3` |

---

## 💡 نصائح:

### ✅ **افعل:**
- استخدم `/` للكسور
- استخدم `√()` أو `sqrt()` للجذور
- استخدم `^` أو الرموز المباشرة للأسس
- اكتب بوضوح مع مسافات

### ❌ **لا تفعل:**
- لا تستخدم صور للمعادلات البسيطة
- لا تستخدم HTML معقد
- لا تنسخ من Word بدون تنظيف

---

## 📋 أمثلة كاملة:

### **مثال 1: سؤال كسور**
```
احسب قيمة 2×333 / 9×1.01 تقريباً ؟

أ. 12
ب. 122
ج. 32
د. 52
```

---

### **مثال 2: سؤال جذور**
```
ما قيمة √(64) ؟

أ. 6
ب. 8
ج. 10
د. 12
```

---

### **مثال 3: سؤال أسس**
```
احسب 2^3 + 3^2 ؟

أ. 13
ب. 17
ج. 19
د. 21
```

---

### **مثال 4: معادلة معقدة**
```
احسب √(x²+y²) / 2 عندما x=3 و y=4 ؟

أ. 2.5
ب. 3.5
ج. 4.5
د. 5.5
```

---

## 🚀 خطوات الاستخدام:

1. **افتح صفحة إضافة درس**
2. **اكتب السؤال في المحرر**
3. **استخدم الرموز الرياضية المذكورة أعلاه**
4. **احفظ الدرس**
5. **افتح التطبيق - سيظهر بشكل رياضي صحيح!**

---

## ⚠️ ملاحظات مهمة:

1. **الكسور المتداخلة:** يتم دعمها تلقائياً
2. **الجذور:** استخدم `√()` أو `sqrt()`
3. **الأسس:** استخدم `^` أو الرموز المباشرة
4. **المسافات:** مهمة للوضوح

---

## 🎨 كيفية كتابة الرموز:

### **في Windows:**
- `²` = Alt + 0178
- `³` = Alt + 0179
- `√` = Alt + 251
- `×` = Alt + 0215

### **في Mac:**
- `²` = Option + 00B2
- `³` = Option + 00B3
- `√` = Option + V
- `×` = Option + Shift + 9

### **أو استخدم:**
- `^2` بدلاً من `²`
- `sqrt()` بدلاً من `√`
- `*` بدلاً من `×`

---

## 📱 كيف يظهر في التطبيق:

**المحرر (لوحة التحكم):**
```
احسب 2×333 / 9×1.01 ؟
```

**التطبيق (الموبايل):**
```
احسب ٢×٣٣٣
      ─────  ؟
      ٩×١٫٠١
```

---

## 🎯 الخلاصة:

المحرر الآن يدعم **جميع** العمليات الرياضية الأساسية:
- ✅ كسور بسيطة ومعقدة
- ✅ جذور تربيعية
- ✅ أسس وقوى
- ✅ معادلات مركبة

**استخدمه بثقة!** 🚀
