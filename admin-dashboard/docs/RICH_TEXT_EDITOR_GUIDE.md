# دليل استخدام محرر النصوص الغني (Rich Text Editor)

## 📝 نظرة عامة

تم إضافة محرر نصوص غني (TinyMCE) لحقول الأسئلة في صفحة إضافة الدروس. هذا المحرر يدعم:

- ✅ **نسخ ولصق من Microsoft Word** مع الحفاظ على التنسيق
- ✅ **كتابة الكسور والمعادلات الرياضية**
- ✅ **التنسيق النصي** (عريض، مائل، ألوان، إلخ)
- ✅ **القوائم والجداول**
- ✅ **الكتابة بالعربية** مع دعم RTL

---

## 🚀 كيفية الاستخدام

### **الطريقة 1: النسخ من Word (الأسهل)**

1. افتح Microsoft Word
2. اكتب السؤال مع الكسور والتنسيق:
   ```
   احسب قيمة ٢×٣٣٣/٩×١٫٠١ تقريباً؟
   ```
3. حدد النص واضغط `Ctrl+C` (نسخ)
4. في صفحة إضافة الدروس، اضغط داخل محرر السؤال
5. اضغط `Ctrl+V` (لصق)
6. ✅ تم! سيظهر السؤال بنفس التنسيق

---

### **الطريقة 2: الكتابة المباشرة**

1. اكتب السؤال مباشرة في المحرر
2. استخدم أزرار التنسيق:
   - **B** = نص عريض
   - **I** = نص مائل
   - **A** = تغيير اللون
   - **≡** = قوائم
3. للكسور والأسس:
   - استخدم `x²` للأسس (مثل: 2²)
   - استخدم `/` للكسور البسيطة

---

## 💡 نصائح مهمة

### **للكسور المعقدة:**

**في Word:**
```
اكتب الكسر باستخدام محرر المعادلات في Word:
Insert → Equation → Fraction
```

**مثال:**
```
٢×٣٣٣
─────  ← يُكتب في Word ثم يُنسخ
٩×١٫٠١
```

---

### **للمعادلات المعقدة جداً:**

إذا كانت المعادلة معقدة جداً (مثل الجذور والتكاملات):
1. استخدم حقل "صورة السؤال" كبديل
2. اكتب المعادلة في Word
3. خذ Screenshot
4. ارفع الصورة

---

## 📊 كيف يعمل النظام؟

### **في لوحة التحكم:**
```
1. تكتب السؤال في المحرر
2. يُحفظ كـ HTML في قاعدة البيانات
   مثال: <p>احسب <strong>٢×٣٣٣</strong> ÷ ٩×١٫٠١</p>
```

### **في التطبيق:**
```
1. يجلب السؤال من قاعدة البيانات (HTML)
2. يعرضه باستخدام react-native-render-html
3. يظهر بنفس التنسيق الأصلي!
```

---

## 🎯 أمثلة عملية

### **مثال 1: سؤال بسيط**

**في Word:**
```
ما هي قيمة ٢ + ٢ ؟
```

**النتيجة في قاعدة البيانات:**
```html
<p>ما هي قيمة ٢ + ٢ ؟</p>
```

---

### **مثال 2: سؤال مع كسر**

**في Word:**
```
احسب قيمة ٢×٣٣٣/٩×١٫٠١ تقريباً؟
```

**النتيجة في قاعدة البيانات:**
```html
<p>احسب قيمة <strong>٢×٣٣٣</strong> ÷ <em>٩×١٫٠١</em> تقريباً؟</p>
```

---

### **مثال 3: سؤال مع تنسيق متعدد**

**في Word:**
```
السؤال: احسب القيمة التقريبية للعبارة التالية:
٢×٣٣٣
─────
٩×١٫٠١

الخيارات:
أ) ٧٤
ب) ٧٥
ج) ٧٦
د) ٧٧
```

**النتيجة:** يُحفظ كـ HTML مع كل التنسيق!

---

## ⚙️ الإعدادات التقنية

### **المكتبات المستخدمة:**

**في لوحة التحكم:**
- `@tinymce/tinymce-react` - محرر النصوص الغني

**في التطبيق:**
- `react-native-render-html` - عرض HTML

---

### **الملفات المُضافة:**

```
admin-dashboard/
  ├── components/
  │   └── RichTextEditor.tsx       ← مكون المحرر
  └── app/subjects/[id]/lessons/
      └── page.tsx                 ← تم التحديث

src/
  └── components/
      └── HtmlRenderer.js          ← مكون عرض HTML
```

---

## 🔧 استكشاف الأخطاء

### **المشكلة: التنسيق لا يظهر في التطبيق**

**الحل:**
1. تأكد من تثبيت `react-native-render-html` في التطبيق
2. استخدم مكون `HtmlRenderer` لعرض السؤال

---

### **المشكلة: الكسور لا تظهر بشكل صحيح**

**الحل:**
1. استخدم محرر المعادلات في Word
2. أو استخدم صورة للمعادلات المعقدة جداً

---

## 📞 الدعم

للمساعدة أو الاستفسارات، راجع التوثيق الكامل أو اتصل بفريق التطوير.

---

**تم التحديث:** ديسمبر 2025
**الإصدار:** 1.0
